NODE_ENV=development
PORT=3000

# Neon Postgres (converted from JDBC)
DATABASE_URL=postgresql://neondb_owner:npg_ekT1f7YqPWgu@ep-plain-bird-a1w9vaa4-pooler.ap-southeast-1.aws.neon.tech/O2O?sslmode=require

# Redis cache disabled by default (only enabled if ENABLE_REDIS_CACHE=true)
ENABLE_REDIS_CACHE=false

# JWT (required for Auth/JwtStrategy)
JWT_SECRET=dev-secret-change-me
JWT_EXPIRES_IN=7d

