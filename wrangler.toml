name = "o2o-backend-gateway"
main = "worker/src/index.ts"
compatibility_date = "2026-01-30"

# Public upstream URL (not a secret)
[vars]
UPSTREAM_URL = "https://alfred-laws-member-must.trycloudflare.com"

# NOTE:
# This deploys a Cloudflare Worker "gateway/proxy", NOT the NestJS app itself.
# Your NestJS backend should be deployed on a Node hosting, and this Worker can proxy to it.

